// End-to-end tests: a real VS Code runs the built extension (dist/ + bin/) against a
// scratch workspace generated by ../../scripts/e2e.mjs.
import { defineConfig } from '@vscode/test-cli';

export default defineConfig({
  files: 'out/e2e/**/*.test.js',
  workspaceFolder: 'out/e2e-workspace',
  version: process.env.VSCODE_VERSION ?? 'stable',
  mocha: { ui: 'tdd', timeout: 90000, color: true },
  launchArgs: ['--disable-extensions', '--disable-workspace-trust'],
});
